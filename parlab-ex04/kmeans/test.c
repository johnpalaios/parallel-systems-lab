#include <stdio.h>
#include <stdlib.h>
#include <mpi.h>

#include "kmeans.h"

int main() {
	double rank_delta, delta = 0;
	int * rank_newClusterSize, * newClusterSize; 
	double * rank_newClusters, *newClusters;
	
}
